using UnityEngine;

public class Platforms : MonoBehaviour
{
    /*public float timerCount;
    private void Update()
    {
        //Look for the platforms names to make them move or remain static
        if (gameObject.name == "MovingPlatform")
        {
            //Make the platform move left and right 
        }
        else if (gameObject.name == "StaticPlatform")
        {
            transform.Translate(Vector3.left * Time.deltaTime * 2);
        }
    }*/
}
